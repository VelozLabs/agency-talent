TEMPORARY STAGING — delete once VelozLabs/nfl-edge exists.

These are the scaffolding files authored for the new private nfl-edge repo, held here
only because the agent container is ephemeral and repo creation was blocked by GitHub
App permissions. The full assembled repo (source + docs + talents + these files, 139
files, one baseline commit) lives at:

  <scratchpad>/nfl-edge-repo

The vendored nfl_edge source is not duplicated here — it is recoverable from the
originally uploaded nfl_edge.zip.
